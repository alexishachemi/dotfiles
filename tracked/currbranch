source ~/.git-prompt.sh
source ~/.bashrc

if [[ $# -ne 0 ]]; then
    echo "Usage: currbranch"
    exit 1
fi

branch=$(__git_ps1 '%s')

if [[ -z $branch ]]; then
    exit 0
fi

if [[ $branch =~ ^[0-9]+- ]]; then
    branch=$(echo $branch | cut -d'-' -f1)
fi

echo " $branch"
